# plugins_preset: "all" # Valid values are "default", "all" or "none".
plugins:
  dbinfo:
    disabled: false
    pkgname: "dbinfo"
    destination: "./internal/infrastructure/rdb/dbinfo"
  enums:
    disabled: false
    pkgname: "enums"
    destination: "./internal/infrastructure/rdb/enums"
  models:
    disabled: false
    pkgname: "models"
    destination: "./internal/infrastructure/rdb/models"
  factory:
    disabled: true
    pkgname: "factory"
    destination: "./internal/infrastructure/rdb/factory"
  dberrors:
    disabled: false
    pkgname: "dberrors"
    destination: "./internal/infrastructure/rdb/dberrors"
  where:
    disabled: false
  loaders:
    disabled: false
  joins:
    disabled: false

psql:
  driver: "github.com/jackc/pgx/v5/stdlib"
  dsn: "postgres://user:P@ssw0rd@localhost:5432/todo-go?sslmode=disable"
  schemas:
    - public
  uuid_pkg: "google"
  except:
    atlas_schema_revisions:
